	<h1><%= "Contact ID: #{params[:id]}" %></h1>
	<h3><%= link_to("Home", contacts_path) %></h3>
	<b><%= @contact.name %></b><br>
	<% @contact.phonenumbers.each do |number| %>
		<%= number.phonenumber %>
	<% end %>
	<br>
	<% @contact.emails.each do |i| %>
		<%= i.email %>
	<% end %>

