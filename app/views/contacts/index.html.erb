	<ul>
		<% @contacts.each do |contact| %>
			<li>
				<%= link_to(contact.name, contact_path(contact)) %>
			</li>
		<% end %>
	</ul>
	<% ("A"..."Z").each do |letter| %>
			<%= link_to(letter, contact_by_letter_path(letter)) %>
	<% end %>
	<%= link_to("Add new", new_contact_path) %>
